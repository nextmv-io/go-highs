version: "2"
run:
  go: "1.22"
linters:
  default: none
  enable:
    - bodyclose
    - containedctx
    - contextcheck
    - decorder
    - dogsled
    - dupl
    - errcheck
    - goconst
    - gocritic
    - gocyclo
    - godot
    - gomodguard
    - govet
    - importas
    - ineffassign
    - lll
    - misspell
    - nakedret
    - nestif
    - nilerr
    - noctx
    - prealloc
    - predeclared
    - revive
    - staticcheck
    - tagliatelle
    - unconvert
    - unparam
    - unused
    - whitespace
  settings:
    dupl:
      threshold: 200
    gocyclo:
      min-complexity: 20
    gomodguard:
      allowed:
        modules:
          - github.com/nextmv-io/sdk
          - github.com/nextmv-io/go-mip
    lll:
      line-length: 120
    tagliatelle:
      case:
        rules:
          json: snake
        use-field-name: true
  exclusions:
    generated: lax
    rules:
      - linters:
          - lll
        path: solver\.go
      - linters:
          - goconst
          - gocyclo
        path: solver_test\.go
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gci
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
      - solver\.go
